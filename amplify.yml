version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
  customHeaders:
    - pattern: "/proposal_template.pptx"
      headers:
        - key: "Content-Type"
          value: "application/vnd.openxmlformats-officedocument.presentationml.presentation"
